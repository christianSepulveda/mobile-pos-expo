import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginBottom: 10,
  },
  label: {
    flex: 10,
    fontSize: 20,
  },
  value: {
    flex: 5,
    textAlign: "right",
    fontSize: 20,
  },
});
