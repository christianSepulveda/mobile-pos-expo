import { StyleSheet } from "react-native";
import { textType } from "../../atoms/AppText";
import { COLORS } from "../../../styles/colors";

export const styles = StyleSheet.create({
  container: {
    height: 50,
    width: "100%",
  },
  textInput: {
    width: "94%",
    height: "100%",
    color: COLORS.whiteSmoke,
  },
});
